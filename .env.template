PORT=PORT
JWT_SECRET=JWT_SECRET
DATABASE_URL=DATABASE_URL
CLOUD_NAME=CLOUD_NAME
API_KEY=API_KEY
API_SECRET=API_SECRET
# Opcional: origenes extra separados por coma (el front local ya viene por defecto)
CORS_ORIGINS=
# Base separada para `npm test` (los tests nunca tocan la de desarrollo)
TEST_DATABASE_URL=
# Opcional: limites de rate limiting. Por defecto 10 logins fallidos/15min y 5 registros/hora
LOGIN_RATE_LIMIT=
REGISTER_RATE_LIMIT=
# En produccion detras de un proxy (Render): poner 1. En local, dejar vacio.
TRUST_PROXY=
